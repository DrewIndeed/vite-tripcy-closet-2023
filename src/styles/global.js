import styled from "styled-components";

export const AppWrapper = styled.main`
  .jump {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
  }

  .app {
    width: 100%;

    .section {
      width: 100%;
      height: 100vh;
      font-size: 8em;
    }
  }
`;
